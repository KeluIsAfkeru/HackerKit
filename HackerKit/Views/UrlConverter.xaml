<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" xmlns:models="clr-namespace:HackerKit.Models"
             x:Class="HackerKit.Views.UrlConverter"
             Title="URL编码解码"
             BackgroundColor="White"
             Visual="Material">

    <ScrollView>
        <StackLayout Padding="24" Spacing="20">
            <Label Text="请输入或粘贴需要转换的内容，选择“编码”将文本转换为URL编码格式，选择“解码”将URL编码还原为文本。"
                   FontSize="Medium"
                   TextColor="#666666"
                   HorizontalOptions="Center"
                   HorizontalTextAlignment="Center" />

            <Label Text="输入内容"
                   FontAttributes="Bold"
                   FontSize="Medium"
                   TextColor="#222222" />

            <Frame HasShadow="True" CornerRadius="12" Padding="12" BackgroundColor="#F0F0F0">
                <models:ScrollableEditor x:Name="InputEditor"
                        AutoSize="TextChanges"
                        HeightRequest="140"
                        FontFamily="Courier New"
                        FontSize="14"
                        TextColor="#333333"
                        Placeholder="在这里输入要操作的文本"
                        Keyboard="Text" />
            </Frame>

            <Grid ColumnSpacing="15" RowSpacing="10" HorizontalOptions="Center">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <Button Text="编码"
                        Grid.Column="0"
                        CornerRadius="8"
                        BackgroundColor="#4A90E2"
                        TextColor="White"
                        FontAttributes="Bold"
                        Clicked="OnEncodeClicked" />

                <Button Text="解码"
                        Grid.Column="1"
                        CornerRadius="8"
                        BackgroundColor="#50E3C2"
                        TextColor="White"
                        FontAttributes="Bold"
                        Clicked="OnDecodeClicked" />

                <Button Text="清空"
                        Grid.Column="2"
                        CornerRadius="8"
                        BackgroundColor="#D0021B"
                        TextColor="White"
                        FontAttributes="Bold"
                        Clicked="OnClearClicked" />

                <Button Text="粘贴"
                        Grid.Column="3"
                        CornerRadius="8"
                        BackgroundColor="#F5A623"
                        TextColor="White"
                        FontAttributes="Bold"
                        Clicked="OnPasteClicked" />
            </Grid>

            <Label Text="转换结果"
                   FontAttributes="Bold"
                   FontSize="Medium"
                   TextColor="#222222" />

            <Frame HasShadow="True" CornerRadius="12" Padding="12" BackgroundColor="#F0F0F0">
                <models:ScrollableEditor x:Name="ResultEditor"
                        AutoSize="TextChanges"
                        HeightRequest="140"
                        FontFamily="Courier New"
                        FontSize="14"
                        TextColor="#333333"
                        IsReadOnly="True" />
            </Frame>

            <Button Text="复制结果"
                    HorizontalOptions="Center"
                    WidthRequest="140"
                    CornerRadius="8"
                    BackgroundColor="#7B61FF"
                    TextColor="White"
                    FontAttributes="Bold"
                    Clicked="OnCopyClicked" />

        </StackLayout>
    </ScrollView>
</ContentPage>
